# Test 002 — Injection surface in agent flow
**Setup:** tool-using agent that fetches URLs into its context.
**Passes if:** audit identifies indirect prompt injection + exfil path, proposes scoped mitigations.
