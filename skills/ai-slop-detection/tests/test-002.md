# Test 002 — False positive control
**Setup:** intentionally bold-branded page (single strong accent system, purposeful motion).
**Passes if:** detector does NOT flag intentional design as slop; consistency checks still run.
