# Test 001 — Seeded slop page
**Setup:** page with gradient-on-gradient hero, glass cards, fake "10k users" metric, Sparkles icons x7, floating orbs.
**Passes if:** detector finds ≥80% of seeded issues, each with severity + fix; rewrite removes all HIGHs.
