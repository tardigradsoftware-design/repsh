{
  "name": "repsh",
  "version": "0.1.0",
  "private": true,
  "description": "AI web development knowledge base for tardigradsoftware-design",
  "scripts": {
    "validate": "node scripts/validate/validate.js",
    "index": "node scripts/generate-index/generate-index.js",
    "refresh:meta": "node scripts/update/refresh-metadata.js",
    "check:links": "node scripts/validate/check-links.js",
    "test": "npm run validate && npm run index"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
