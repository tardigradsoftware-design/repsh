version: 2
updates:
  # GitHub Actions pinning (supply-chain hygiene for CI itself)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    labels: ["ci", "security"]
